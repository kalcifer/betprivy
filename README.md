BetPrivy
========

Setup
-----
* Clone BetPrivy
* Install Meteor
```
curl https://install.meteor.com/ | sh
```
* Install iron-router
```
sudo npm install -g meteorite
mrt add iron-router
```

* Run `meteor`
 
That's it! 
